name: sync-debian

on:
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:   # 手动触发按钮

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write   # 推送镜像所需权限

    steps:
      - name: Login to GitHub
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: pull and push
        run: |
          docker pull docker.io/gitlab/gitlab-ce:latest && docker tag docker.io/gitlab/gitlab-ce:latest ghcr.io/vansour/gitlab-ce:latest && docker push ghcr.io/vansour/gitlab-ce:latest
